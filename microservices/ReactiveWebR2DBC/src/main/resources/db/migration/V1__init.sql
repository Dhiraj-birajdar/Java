CREATE TABLE student(
                     id SERIAL NOT NULL PRIMARY KEY,
                     firstname VARCHAR (100),
                     lastname VARCHAR (20),
                    address VARCHAR(100),
                    phone VARCHAR(50),
                     age integer
);